########################################################
# package:    Advandz
# filename:   lighttpd.conf
########################################################

$HTTP["url"] =~ "^/config" {
    url.access-deny = ("")
}

$HTTP["url"] =~ "^/components" {
    url.access-deny = ("")
}

$HTTP["url"] =~ "^/helpers" {
    url.access-deny = ("")
}

$HTTP["url"] =~ "^/language" {
    url.access-deny = ("")
}

$HTTP["url"] =~ "^/lib" {
    url.access-deny = ("")
}

$HTTP["url"] =~ "^/logs" {
    url.access-deny = ("")
}

$HTTP["url"] =~ "^/vendors" {
    url.access-deny = ("")
}

url.rewrite-once= (
        "^\/(app|assets|cache|components|config|debugger|helpers|language|lib|logs|plugins|vendors)(.*)$" => "$0",
        "^/(.*)" => "/index.php"
)